# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Local knowledge base (thesis drafts, lab notes, personal/academic) — publish when complete
docs/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock
#poetry.toml

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   pdm recommends including project-wide configuration in pdm.toml, but excluding .pdm-python.
#   https://pdm-project.org/en/latest/usage/project/#working-with-version-control
#pdm.lock
#pdm.toml
.pdm-python
.pdm-build/

# pixi
#   Similar to Pipfile.lock, it is generally recommended to include pixi.lock in version control.
#pixi.lock
#   Pixi creates a virtual environment in the .pixi directory, just like venv module creates one
#   in the .venv directory. It is recommended not to include this directory in version control.
.pixi

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Abstra
# Abstra is an AI-powered process automation framework.
# Ignore directories containing user credentials, local state, and settings.
# Learn more at https://abstra.io/docs
.abstra/

# Visual Studio Code
#  Visual Studio Code specific template is maintained in a separate VisualStudioCode.gitignore 
#  that can be found at https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore
#  and can be added to the global gitignore or merged into this file. However, if you prefer, 
#  you could uncomment the following to ignore the entire vscode folder
# .vscode/

# Ruff stuff:
.ruff_cache/

# PyPI configuration file
.pypirc

# Cursor
#  Cursor is an AI-powered code editor. `.cursorignore` specifies files/directories to
#  exclude from AI features like autocomplete and code analysis. Recommended for sensitive data
#  refer to https://docs.cursor.com/context/ignore-files
.cursorignore
.cursorindexingignore
.cursor/

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/

# Local workspace configurations and experiments
.config/
.expermients/
.experiments/

# Copilot instructions
.github/copilot-instructions.md

# ChaosGen runtime artifacts (may contain telemetry / incident data)
history.db
history.db-wal
history.db-shm
last_report.json
promoted_scenarios.json
demo-report.json
demo-report-resilience.json
**/demo-report*.json
.chaosgen/
settings.local.yaml
*.local.yaml

.thesis/
# Trained ML baselines (local lab artifacts) — keep directory via .gitkeep
models/*
!models/.gitkeep
*.joblib

last_verdict.json

# Public labeled telemetry dumps (local only — do not commit)
data/public-datasets/

# Obscura agent screenshot / PDF vault (local only)
scratch/
!scratch/obscura/.gitkeep

# Chaos Toolkit local run artifacts (journals, dry-run manifests, journals)
# Covered by scratch/; keep explicit so accidental paths outside are obvious
scratch/ctk/
scratch/ctk/experiments/
scratch/ctk/journals/

# Local GUI / operator dumps
*.ctk.local.json
serverless-boundary-*.json

# --- Privacy / academic / personal (NOT for public OSS) ---
# Full docs/ is ignored above; keep these for paths outside docs/
**/*Thesis*.pdf
**/*_Thesis.pdf
**/NguyenHuynh*
**/*CSIU*
**/*ITCSIU*
.thesis/
private/

# Operator-local settings with real lab endpoints (copy from examples/*.yaml)
examples/*-local.yaml
examples/registry-vm-settings.local.yaml
scripts/*_local.py

# --- Public-repo hygiene (OSS) ---
# OS / editor debris
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*~
.idea/
.vscode/
*.code-workspace

# Local AI / agent tooling (never publish)
.claude/
.agent/
.agents/
.codebase-memory/
*.codebase-memory*

# Chaos Toolkit / runtime logs at repo root
chaostoolkit.log
journal.json
experiment.json

# Generated diagram visual-check screenshots & harness
**/visual-check*.png
**/visual-check*.html
**/visual-check*.json

# Local secrets / operator overrides (never commit real tokens)
.env.*
!.env.example
secrets.yaml
**/secrets.local.*
*.pem
*.p12
kubeconfig.local

# Keep models/ out of git except placeholder
# (pattern models/* + !models/.gitkeep is defined above)

