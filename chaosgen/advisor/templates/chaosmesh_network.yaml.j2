apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: {{ experiment_name | lower | replace(" ", "-") }}
  namespace: {{ namespace | default("default") }}
  labels:
    app.kubernetes.io/managed-by: {{ managed_by | default("chaosgen") }}
    chaosgen.io/ephemeral: "{{ ephemeral | default("true") }}"
    chaosgen.io/experiment: {{ experiment_label | default("chaosgen") }}
{% if run_id %}
    chaosgen.io/run-id: "{{ run_id }}"
{% endif %}
spec:
  action: {{ action | default("delay") }}
  mode: {{ mode | default("one") }}
  selector:
    namespaces:
      - {{ namespace | default("default") }}
    labelSelectors:
      {{ label_key | default("app") }}: {{ target_name }}
  {% if action == "delay" %}
  delay:
    latency: "{{ latency | default('100ms') }}"
    jitter: "{{ jitter | default('0ms') }}"
    correlation: "{{ correlation | default('0') }}"
  {% elif action == "loss" %}
  loss:
    loss: "{{ loss_percent | default('10') }}"
    correlation: "{{ correlation | default('0') }}"
  {% endif %}
  duration: "{{ duration }}"
