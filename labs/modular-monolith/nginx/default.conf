server {
    listen 80;
    server_name _;

    location /health {
        return 200 'ok';
        add_header Content-Type text/plain;
    }

    location / {
        return 200 'chaosgen modular monolith';
        add_header Content-Type text/plain;
    }
}
