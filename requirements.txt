# Core
PyYAML>=6.0
pydantic>=2.0.0
transitions>=0.9.0
requests>=2.25.0
PySide6>=6.5.0
PySide6-Fluent-Widgets>=1.5.0

# ML Pipeline
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0
matplotlib>=3.7.0

# LLM Integration
ollama>=0.3.0
instructor>=1.0.0

# Template Engine
Jinja2>=3.1.0

# Kubernetes inject (official client; kubectl remains fallback)
kubernetes>=28.1.0

# Chaos Toolkit — canonical experiment runner (UCAL executor).
# Pin (==): CI matrix includes Python 3.10; chaostoolkit 1.20+ requires 3.12+.
# Bump only after dropping 3.10 or verifying the dual-version matrix.
chaostoolkit==1.19.0
chaostoolkit-kubernetes==0.39.0

# MODIFIED: Added API Sidecar & Web UI dependencies (Phase 2A)
fastapi>=0.110
uvicorn[standard]>=0.27

# Chaos Mesh (in-cluster CRDs used by chaosk8s.chaosmesh.*) is NOT a pip package.
# Pin the Helm chart / operator version in docs/dependency-pins.md when installing
# into a lab cluster — keep that pin in sync with chaostoolkit-kubernetes above.
